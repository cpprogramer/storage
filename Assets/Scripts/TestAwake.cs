using UnityEngine;

public class TestAwake : MonoBehaviour
{
    private void Awake() => Debug.Log( "HERE" );

    // Start is called before the first frame update
    private void Start() {}

    // Update is called once per frame
    private void Update() {}
}